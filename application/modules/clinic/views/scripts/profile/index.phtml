<div class="form-horizontal">
    <? if (!$this->clinic->description): ?>
        <div class="alert alert-block">
            <h4 class="alert-heading">Krok 1</h4>
                <a href="<?= $this->url(
                    array(
                        'action'        => 'edit-description',
                        'controller'    => 'profile',
                        'module'        => 'clinic',
                    )
                ); ?>">Uzupełnij opis swojej kliniki</a> widoczny dla Twoich potencjalnych pacjentów oraz dodaj zdjęcia.
        </div>
    <? endif;?>
    <? if (count($this->clinic->services) < 1): ?>
        <div class="alert alert-block">
            <h4 class="alert-heading">Krok 2</h4>
            <a href="<?= $this->url(
                array(
                    'action'        => 'edit-services',
                    'controller'    => 'services',
                    'module'        => 'clinic',
                )
            ); ?>">Dodaj usługi</a> swojej kliniki, aby być widocznych  w naszym katalogu.
        </div>
    <? endif;?>
    <? if (!$this->clinic->logoDir): ?>
        <div class="alert alert-block">
            <h4 class="alert-heading">Krok 3</h4>
            <a href="<?= $this->url(
                array(
                    'action'        => 'edit-logo',
                    'controller'    => 'profile',
                    'module'        => 'clinic',
                )
            ); ?>">Wgraj logo</a> swojej kliniki.
        </div>
    <? endif;?>
</div>
<div class="alert alert-block alert-info">
    Po premierze serwisu w tym miejscu będziesz miał dostęp do swoich rezerwacji złożonych przez pacjentów oraz
    statystyk odwiedziń i zapytań o rezerwacje.
</div>