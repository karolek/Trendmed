<div id="clinicProfile">

<div class="row">
	
	<div class="left">
		<h1><?= $this->clinic->name; ?></h1>
	    <address>
	        <?= $this->clinic->streetaddress; ?>, <?= $this->clinic->postcode; ?> <?= $this->clinic->city; ?>,
	        <?= $this->clinic->province; ?>
	    </address>
    </div>
        
    <div class="right rating">
        <span class="count">86%</span>
        <a href="#">Fav</a>
        <big>Rating</big><br/ >
		<small>based on YY reviews</small>
    </div>
    
    <div class="globalClear"></div>
    
    <ul class="thumbnails">
        <li class="thumbnail">
            <?= $this->ShowClinicLogo($this->clinic); ?>
        </li>
        <? foreach($this->clinic->photos as $photo): ?>
        <?= $this->ShowClinicPhoto($photo, 'small'); ?>
        <? endforeach; ?>
    </ul>
    
    <div class="hidden" style="position: absolute;" id="image-holder"> </div>
    
    <script>
	    $(document).ready(function() {
	       $(".photo-small").mouseover(function() {
	            var src = $(this).attr("src");
	            var src =src.replace("small", "medium");
	
	            $("#image-holder").removeClass('hidden').html('<img src=' + src + '>');
	       });
	       $(".photo-small").mouseout(function() {
	           $("#image-holder").addClass('hidden');
	       })
	    });
	</script>
    
    <a id="buttonBook" href="#" class="btn btn-primary"><?= $this->translate('Book now'); ?></a>
    
    <?= $this->clinic->description; ?>
    
</div>





<div class="row">
    <h2><?= $this->translate('Promotions'); ?></h2>
    <?= $this->clinic->customPromos; ?>
</div>
<div class="row" id="clinic-grouppromotion">
    <img src="/img/group_promotion.png" />
</div>


<? if (count($this->clinic->services) >0 ) : ?>
<div class="row">
    
    	<h2>Services of <?= $this->clinic->name; ?></h2>
    	
        <table class="table">
            <thead>
                <tr>
                    <th><?= $this->translate('Service name'); ?>:</th>
                    <th><?= $this->translate('Price range'); ?>:</th>
                    <th><?= $this->translate('Description'); ?>:</th>
                    <th><?= $this->translate('Book'); ?>:</th>
                </tr>
            </thead>
            <tbody>
                <? foreach ($this->clinic->services as $service): ?>
                    <tr>
                        <td><?= $service->category->name; ?></td>
                        <td><?= $service->priceMin; ?> - <?= $service->priceMin; ?> Euro</td>
                        <td><?= $service->description; ?></td>
                        <td><input type="checkbox" name="book[]" value="<?=$service->id; ?>"></td>
                    </tr>
                <? endforeach; ?>
            </tbody>
        </table>
</div>
<? endif; ?>

</div>