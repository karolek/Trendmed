<a id="addNewService" class="btn" href="<?= $this->url(array('action' => 'edit-services')); ?>"><?= $this->translate('Add new service'); ?></a>

<? if (count($this->services) < 1 ): ?>
<div class="alert alert-info">
    <strong>Nie dodałeś żadnych usług</strong><br />
    Usługi pozwolą Twoim potencjalnym pocjentom odnaleźć Cię w naszym katalogu.
    Dodaj usługi wykonywane w Twojej placówce.
</div>
<? endif; ?>

<? if (count($this->services) > 0 ): ?>
    <table id="clinicServicesTable" class="table">
        <thead>
            <tr>
                <th>Kategoria</th>
                <th>Opis</th>
                <th>Cena od - do</th>
                <th>Opcje</th>
            </tr>
        </thead>
        <tbody>
    <? foreach($this->services as $service): ?>
        <tr>
            <td><?= $service->category->name; ?></td>
            <td width="600"><?= $service->description; ?></td>
            <td><?= $service->pricemin; ?> - <?= $service->pricemax; ?></td>
            <td>
                <div class="btn-group">
                    <button class="btn"><a href="<?= $this->url(
                        array(
                            'action' => 'edit-services',
                            'controller' => 'services',
                            'module' => 'clinic',
                            'id' => $service->getId()
                        ), 'default', true); ?>"><?= $this->translate('Edit'); ?></a></button>
                    <button class="btn dropdown-toggle" data-toggle="dropdown">
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu">
                        <!-- dropdown menu links -->
                        <li>
                            <a href="<?= $this->url(array('action' => 'manage-service-photos', 'controller' => 'services',
                                'id' => $service->getId())); ?>"><?= $this->translate('Manage photos'); ?></a>
                        </li>
                        <li>
                            <a class="confirm" href="<?= $this->url(array('action' => 'delete-service',
                                'id' => $service->getId())); ?>"><?= $this->translate('Delete'); ?></a>
                        </li>
                    </ul>
                </div>

            </td>
        </tr>
    <? endforeach; ?>
        </tbody>
    </table>
<? else: ?>

<? endif; ?>